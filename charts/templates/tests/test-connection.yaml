apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "site-manager.fullname" . }}-test-connection"
  labels:
    {{- include "site-manager.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "site-manager.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never

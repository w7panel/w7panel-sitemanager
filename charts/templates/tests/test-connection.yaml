{{- if and (eq (include "tradition-tool.gatewayEnabled" .) "true") .Values.service.enabled }}
apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "tradition-tool.fullname" . }}-test-connection"
  labels:
    {{- include "tradition-tool.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "tradition-tool.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
{{- end }}

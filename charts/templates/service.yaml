apiVersion: v1
kind: Service
metadata:
  name: {{ include "site-manager.fullname" . }}
  labels:
    {{- include "site-manager.labels" . | nindent 4 }}
    allow-proxy: "true"
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
    {{- if .Values.webhook.enabled }}
    - port: {{ .Values.webhook.port }}
      targetPort: webhook
      protocol: TCP
      name: webhook
    {{- end }}
  selector:
    {{- include "site-manager.selectorLabels" . | nindent 4 }}

apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "site-manager-nginx.fullname" . }}-test-connection"
  labels:
    {{- include "site-manager-nginx.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "site-manager-nginx.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never

apiVersion: v1
kind: Service
metadata:
  name: {{ include "site-manager-nginx.fullname" . }}
  labels:
    {{- include "site-manager-nginx.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "site-manager-nginx.selectorLabels" . | nindent 4 }}
